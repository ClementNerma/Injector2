{
	"private": true,
	"scripts": {
		"build:domains": "pnpm esbuild scripts/inject.ts --bundle --outfile=dist/inject.js --sourcemap=inline --target=esnext --format=iife",
		"build": "pnpm build:domains && rm -f Injector2.zip && zip -r Injector2.zip manifest.json icons/ dist/"
	},
	"devDependencies": {
		"@types/chrome": "^0.0.237",
		"esbuild": "^0.17.19"
	}
}